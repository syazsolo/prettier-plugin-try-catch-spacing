{
  "name": "prettier-plugin-try-catch-spacing",
  "version": "1.0.1",
  "description": "Enforce visual separation in error handling. A Prettier plugin that automatically injects a whitespace gap before the closing brace of a try block.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "prettier",
    "prettier-plugin",
    "try-catch",
    "formatting",
    "style"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.7.4",
    "vitest": "^4.0.15"
  }
}
